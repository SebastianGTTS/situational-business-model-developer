{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsArtifactModule",
  "title": "Create new artifact module schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the module.",
      "type": "string",
      "x-prompt": "What is the name of the new module?"
    }
  },
  "required": ["name"]
}
