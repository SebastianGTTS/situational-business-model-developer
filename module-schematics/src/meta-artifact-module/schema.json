{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsMetaArtifactModule",
  "title": "Create new meta artifact module schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the module.",
      "type": "string",
      "x-prompt": "What is the name of the new module?"
    }
  },
  "required": ["name"]
}
